# Workflow desactivado - nueva arquitectura usa index.html estático
# Para reactivar, descomentar el schedule
name: Generar Informe Diario de Mercado

on:
  workflow_dispatch: # solo manual ahora

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Workflow desactivado. La nueva app usa index.html estático."
